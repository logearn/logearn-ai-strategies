# 策略代码规范（打分版契约）

目的：让**同一份策略代码**既能在 review 工具里回放调参、又能原样贴到 logearn 平台跑，且打分/硬否决口径统一。只要新策略/改动符合本规范，就与 review 工具解耦——不会再犯 `f is not defined`、打分项被扫成硬否决、score 超 100、防雷指标隐形失效之类的坑。

自动校验：`review/src/lib/strategySpec.js`（纯函数）。三处复用——
- 策略回放看板：编辑框实时提示违规 + 一键修正；
- `review/tests/strategy-spec.test.js`：单测规则；
- `review/tests/lint-strategies.js`：扫真实的 `*/code-score.js`（`node tests/lint-strategies.js`）。

模板骨架：`策略模板/strategy-template.js`（复制即用，已内置下列全部约定）。

---

## 硬性规则（违反 = error，看板红色，可能线上直接报错）

### 1. 只依赖 ctx；用 f() 必须带垫片
策略运行时**只保证有 `ctx`**（和全局 `Date`）。`f / __emit` 只有 review 注入，线上没有。
- 若用了 `f('字段')`（review「发送到策略」生成的打分因子就是这种），**顶部必须放标准 f 兼容垫片**（`var f = typeof f==='function' ? f : (…从 ctx 现算…)`）。垫片真源在 `strategySpec.js` 的 `F_SHIM`，看板「修正」会自动插入。
- 用 `var f`（不是 const/let），否则和 review 注入的同名参数冲突报"已声明"。

### 2. VETO_NAMES 必须显式列出
**禁止** `const VETO_NAMES = new Set(ALL_CHECKS.map(c => c[0]))`——那样把每一项都当硬否决，后续追加的打分因子也被扫成一票否决（1.5段踩过）。只显式写真正的硬否决名字：
```js
const VETO_NAMES = new Set(['平台', '市值', '时长h', /* …只列硬否决… */])
```
挪一项成打分因子：从这个名单里删掉它的名字即可（ALL_CHECKS 那行保留）。

### 3. 总分行写 `score >= CUTOFF`
不要写成 `score >= score`（恒真笔误，会让「总分」check 的通过标记失真）。

---

## 打分口径规则（违反 = warn，看板黄色）

### 4. 归一化分母只夹正权重
```js
total += s * weight
wsum  += Math.max(0, weight)          // 不能写 wsum += weight
const score = wsum > 0 ? total / wsum * 100 : 0   // 0~100 归一化
```
邪恶阵营用负权重（命中危险区扣分），它的"最好情况"是 s=0（贡献 0），不是拿到 weight。分母若把负权重也算进来，会被拉小、score 冲过 100（实测跑出过 120）。`CUTOFF` 是 0~100 的百分比阈值。

### 5. 必须输出 SCORE 标记
review 只消费 `ctx.log` 里的机器可解析标记：
```
SCORE=<0-100> VER=<版本号> GRADE=<S|A|->
```
缺它，回放看板拿不到分数，"总分 vs 收益"等分析全失效。

### 6. ALL_CHECKS 里 name 唯一
每条 check 的第一个元素（name）在 ALL_CHECKS 内不能重复——review 的因子聚合按 name 分组，重名会被合并覆盖。（注意别用 20 字符截断把不同字段截成同名。）

---

## 约定（非强制，但强烈建议）

- **ALL_CHECKS 元组格式统一**：`[name, value, weight, lo0, lo1, hi1, hi0, actualDisplay?, expectLabel?]`。梯形 `trap(value, lo0,lo1,hi1,hi0)`：`[lo1,hi1]` 满分 1，两侧线性过渡，界外 0，缺失 0；零宽 `lo1===hi1` 退化成布尔阶跃。
- **权重给真实值**，别用 0/1 占位——占位权重会让"以为在打分"的因子隐形失效。
- **不重复计分**：同一底层值（如 `gmgn.dev.top_10_holder_rate` 与 `gmgn.stat.top_10_holder_rate` 常同值）别加两条。
- **单位**：`gmgn.*` 的 rate/percentage 原始是 0-1 小数，review 特征层与 f 垫片都 ×100（见 `data.js` 的 `PERCENT_FRACTION_FIELDS`）→ 打分区间按**百分比**写；硬否决若直接读原始 `ctx.gmgn.*` 要**自己 ×100**。
- **时间**：当刻时间一律 `Date.now()`（review 冻结在买点、线上=买点，等价）；**禁止读买入后才有的结果字段**（returnMax / post_buy_* 等未来函数）。
