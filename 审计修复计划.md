# 全项目审计修复计划（2026-07-29）

> 来源：一次只读全量审计（`review/src/lib`、`review/src/ui`、根目录各策略 js、`CLAUDE.md`、`review/tests`）。
> 审计基线：`node tests/run-tests.js` → 626/626 通过。
> 本文件是**执行清单**：每条带「问题 / 位置 / 改法 / 验收」，做完在标题后打 ✅ 并补一行实际改动说明。
> review/ 目录下的改动同时要同步到 [review/readme.md](review/readme.md)（项目约定）。

## 执行顺序总览

| 阶段 | 条目 | 判断依据 |
|---|---|---|
| P0 · 立即修 | 1~5 | 已复现的错数/挂死/静默丢数据，且改动小 |
| P1 · 排期修 | 6~11 | 潜伏炸弹、竞态、方法论缺口 |
| P2 · 清理 | 12~15 | 死代码、文档矛盾、仓库卫生 |

每完成一条：`node tests/run-tests.js` 必须仍然全绿（数量只增不减）。

---

# P0 ✅ 全部完成（2026-07-29）

测试：626 → 634 全绿（新增 8 条，其中 6 条是专门守住本轮 bug 的回归线）。
每条修复都做过「回退修复→新测试必须变红」的反向验证，记录在各条的验收行。

## 1. AUC 方向常量 `'low'` ≠ `'lower'`，邪恶阵营 CI 被整体镜像 ✅

- **位置**：[review/src/lib/auc.js:37,39](review/src/lib/auc.js) + [review/src/lib/utils.js:306](review/src/lib/utils.js)
- **问题**：`auc.js` 产出 `direction = 'high'|'low'`，点估计按它翻转成 `1-aucHigh`；但 `utils.js` 的 `rankAuc` 判的是 `direction === 'lower'`，`'low'` 落进 else → bootstrap 全程按正向算。**点估计翻转了、CI 没翻转**，两者永远差一次镜像。
- **后果**：所有"值小更好"的字段（＝整个邪恶阵营）CI 落在 0.5 以下；`proAnalytics` 的 `aucVerdict` 把真正有效的因子标成「反向有效」——文案意思完全相反。实测 `auc=0.7984 / ci=[0.144,0.269]`，点估计落在 CI 之外。
- **不受影响的部分**：镜像关于 0.5 对称，`significant`（CI 不跨 0.5）与 `pApprox`（用半宽）恰好不变。
- **改法**：项目里存在两套方向词汇（`utils` 内部 `'higher'/'lower'`，`auc.js`/UI/持久化因子池 `'high'/'low'`）。**不改词汇、在 utils 边界归一**——加 `isLowerDirection(d)` 同时认两种写法，`rankAuc`/`computeROC` 都走它。理由：`direction` 会随因子池存进 localStorage，改词汇会让存量数据方向反转。
- **验收 ✅**：`utils.js` 加 `isLowerDirection(d)`（认 `'lower'|'low'`），`rankAuc`/`computeROC` 都走它，两处调用方一个字没改。`tests/auc.test.js` 给原有的「反向可分」用例补了 CI 断言，另加一条弱 `low` 信号用例（n=300，贴近真实候选强度）。反向验证：把归一函数改回只认 `'lower'`，这两条立刻红（`ci=[0,0]`、`auc=0.798 / ci=[0.144,0.266]`）。

## 2. 两处 worker 缺 `error` 监听 → 扫描永久挂死 + worker 泄漏 ✅

- **位置**：[review/src/ui/factorLab/workerPool.js:63](review/src/ui/factorLab/workerPool.js)（扫描）、`:148`（边际ρ）
- **问题**：只挂了 `message`；同文件 `:194`/`:231` 是有 `error` 监听的。
- **后果**：worker 加载失败 / OOM / 结构化克隆抛错时，`next(worker)` 永不调用 → 外层 Promise **永不 resolve**，UI 永远「扫描中…」；且 [useFactorScan.js:94](review/src/ui/factorLab/useFactorScan.js) 的「回退主线程串行」catch 永远进不去（不是 reject，是 hang）。
- **改法 ✅**：两处照 `:194` 的写法补 `error` 监听：`terminate()` 该 worker、`dead++`、推进收尾；全部 worker 都异常退出且还有批次没回包时整体 reject，让上层 fallback 生效。
  - **实现时踩到的坑**：第一版判据写成 `bi < batches.length`（`bi` 是**派发游标**，派发那一刻就加过了），于是"派发出去、worker 当场炸了"被误判成"跑完了"，照样 resolve。改成数**实际回包的批次** `doneBatches`。这条被新测试当场抓住。
- **验收 ✅**：新增 `tests/worker-pool.test.js`（4 条，用假 Worker 精确控制失败时机 + 2 秒 `Promise.race` 超时兜底）：全死→reject、部分死→其余跑完、边际ρ 同上、正常路径不串阵营。反向验证：摘掉 `error` 监听后 3 条直接 TIMEOUT。

## 3. 候选导出 TSV 的「CI下/CI上」两列恒为 `-` ✅

- **位置**：[review/src/lib/factorScanExport.js:40-41](review/src/lib/factorScanExport.js)
- **问题**：写的是 `Array.isArray(c.ci) ? c.ci[0] : '-'`，但 `bootstrapAucCI` 返回的是 `{lo, hi}` 对象（[utils.js:293](review/src/lib/utils.js)），`factorLab.js:254` 原样透传。`Array.isArray({lo,hi})` 永远 false。
- **后果**：这个导出存在的意义就是补齐 UI 藏起来的「方向/coverage/**CI**」，结果 CI 从来没有过值。
- **改法 ✅**：抽出 `ciBounds(ci)`，`{lo,hi}` 与 `[lo,hi]` 都认，`candidateRow` 统一走它。
- **验收 ✅**：fixture 改成 `{lo,hi}`（生产真实形状），新增 3 条断言——CI 两列真的填上 0.61/0.67、老数组格式向后兼容、`ci:null` 时落成 `-` 不抛错。

## 4. 测试盲区：两条测试的 fixture/取样正好掩盖了第 1、3 条 ✅

- **位置**：[review/tests/factor-scan-export.test.js:7,14](review/tests/factor-scan-export.test.js)、[review/tests/auc.test.js:34-52](review/tests/auc.test.js)
- **问题 A**：export 测试的 fixture 写 `ci: [0.61, 0.67]`（数组），生产是 `{lo,hi}` → `Array.isArray` 在测试里为真、生产里恒假；且只断言表头含 'CI下'/'CI上'，没有一条检查值。
- **问题 B**：auc 测试的 CI 守门用例三个 seed 全走 `high` 分支（随机数据 AUC 贴着 0.5）；唯一构造出强 `low` 方向的用例只断言 `direction` 和 `auc`，**一个字没碰 `ci`**。两条用例盲区正好互补。
- **改法 ✅**：fixture 换成 `{lo,hi}` + 补值断言；auc 用例补 `low` 方向的 CI 包含性断言（强/弱两档各一条）。
- **验收 ✅**：两组回退验证都做了，见第 1、3 条。

## 5. `1.5段策略` 四道 gmgn 风控闸门「缺失即放行」 ✅

- **位置**：[1.5段策略/code.js:74-81](1.5段策略/code.js)
- **问题**：`(gmgnStat.top_10_holder_rate ?? 0) * 100` 等四个字段（前10持仓/创建者持仓/内鬼占比/开发团队持仓）全用 `?? 0`，判定又都是 `< LIMIT` → 缺失必然通过。同文件其它字段用的是保守默认：`shit_volume ?? 999`、`new_volume ?? 999`、`avg_price_deviation_pct ?? -999`。
- **后果**：`CLAUDE.md` 写明 gmgn 数据约四成缺失 → **约四成的币这四道风控静默全部放行**，回测里看不出来。空字符串同理（`'' ?? 0` → `''` → `''*100 = 0`）。
- **改法 ✅**：抽 `gmgnPct(v)` —— **三层兜底缺一不可**，这点实测过：
  - `?? 999` 只挡 undefined/null，挡不住空字符串（`'' ?? 999` 得到 `''`）；
  - **`Number('')` 和 `Number(null)` 都等于 0**，所以光靠 `Number + isFinite` 一样会被判成"0% 持仓"放行（第一版就是这么写的，跑对照表当场露馅）；
  - gmgn 这几个字段有时是字符串数字 `'0.154'`，所以也不能直接 typeof 判 number。
  最终：先显式判 `undefined/null/''` → 999，再 `Number` + `isFinite` → 否则 999。跟 `auc.js` 的 `collectAucSamples` 同一套口径。
- **附带 ✅**：[强势盘策略/code.js:77](强势盘策略/code.js) expect `'20~30'` → `'15~30'`（与代码一致）；`:85` `'> 60'` → `'< 60'`（方向写反）。另外发现 [1.5段策略/code.js:198](1.5段策略/code.js) 的「创建者持仓」expect 写死 `'<1'`，而 `CREATOR_HOLD_RATE_LIMIT` 早已是 `0.5` —— 四条 expect 一并改成**直接引用常量**（`` `<${LIMIT}` ``），以后不会再飘。
- **验收 ✅**：`node --check` 双双通过；缺失/null/空串/字符串数字/正常/超标 六种输入的对照表实测（旧口径前三种全部放行 → 新口径全部拦截，正常值行为不变）。

---

# P1 ✅ 全部完成（2026-07-29）

测试：634 → 641 全绿。改动记录见 [review/readme.md §23.3](review/readme.md)；第 9 条另见 §9.1。

## 6. IndexedDB 连接只开不关 ✅

- **位置**：[review/src/lib/dataStore.js](review/src/lib/dataStore.js)（7 处 `openDb()`、0 处 `close()`；对照 [fsStore.js:119](review/src/lib/fsStore.js) 是有 close 的）
- **后果**：现在不炸；`DB_VER` 一升级，常驻连接会让 `onupgradeneeded` 被 blocked 且无 `onblocked` 处理 → 表现为"打不开数据也不报错"。
- **改法**：模块内缓存单个 db promise（懒加载复用）+ 加 `onversionchange` 主动 close，比逐函数 `finally close` 更省开销。

## 7. `Math.max(...array)` 三处潜伏 RangeError ✅

- **位置**：[review/src/lib/compare.js:36,47](review/src/lib/compare.js)、[review/src/lib/strategyReplayLogic.js:61](review/src/lib/strategyReplayLogic.js)
- **说明**：同类坑已在 [factorLab.js:1090](review/src/lib/factorLab.js) 修过并写了注释，这三处没同步。约 10 万样本触发。
- **改法**：改 `reduce`。

## 8. 扫描/边际ρ 不可取消，旧结果覆盖新结果 ✅

- **位置**：[review/src/ui/factorLab/useFactorScan.js:89,233](review/src/ui/factorLab/useFactorScan.js)
- **问题**：底层 `workerPool` 支持 `opts.signal`，但两个调用方都没传，也没有 runId stale 判定。
- **后果**：扫描要跑几十秒，期间改筛选再点一次 → 先发后到时旧候选表覆盖新的；而 `setScanThreshold` 记的是回调那一刻的最新值，`staleScan`（`:184`）因此**判不出过期**，用户看到"看起来是新的、实际来自旧数据"的表。
- **改法**：每个入口持一个 AbortController（新调用先 abort 旧的）+ 自增 runId，写 state 前判 stale。

## 9. held-out 边际ρ 的区间窗口仍挖自全样本 ✅（按决策只做了 B：订正文档口径）

- **位置**：[review/src/lib/factorLab.js:618](review/src/lib/factorLab.js)（`computeHeldOutDeltaRho`）、`:786`（`recommendFactorPath` 贪心）
- **问题**：`deriveRows` 确实只取 train 段，但 `candidate.interval` 的 lo/hi 来自**全样本**扫描（`scanIntervalCore` 在约 441 个候选窗口里挑最优，winner's curse 主要来自这一步）。readme §9 说的"train 推边界"对**梯形核心**成立，对**区间窗口**不成立。
- **实测（诚实记录）**：纯噪声 40 组对照（n=400）——现状 mean deltaTest = −0.0191 / >0 占比 53%；对照组（interval 也只挖 train）−0.0166 / 50%。**结构性泄漏存在，但这个尺度上没测出统计显著的偏差**。
- **改法（二选一，先做 B）**：A) 让 `computeHeldOutDeltaRho` 在 train 段重挖一次 interval（每候选多扫一遍，有性能代价）；B) 先把 readme §9 措辞改准确，别让人以为 deltaTest 完全无偏，并在 UI 提示里标注。
- **决策**：先做 B（改文档口径），A 留作后续 spike——因为实测偏差不显著，不值得先付性能代价。

## 10. `verifyParity` 单向自检：review 缺失时无条件判 `ok` ✅

- **位置**：[review/src/lib/onlineExport.js:975](review/src/lib/onlineExport.js)
- **问题**：`if (rMiss) return { status: 'ok' }` —— review 侧算不出值时，无论线上算出什么都判「一致」。
- **后果**：线上派生块在某个 ctx 上算出 review 不会产生的值（门槛没对齐）时 100% 漏检。而这是有代价的：因子满分区间若覆盖低值区，线上给分、回测记 0 分，同一个 cutoff 两边含义就不同。
- **改法**：新增 `missing_review` 状态单独统计并在面板展示，不混进 `ok`。

## 11. `assignFoldsByToken` 按组轮转不看组大小 ✅

- **位置**：[review/src/lib/factorLab.js:961](review/src/lib/factorLab.js) 的 `pos % K`
- **问题**：按 token 分组防串样本是对的，但热门币一天几十条信号时，某折会吃到远超 1/K 的样本，另一折可能因 `test.length < 5` 被整折丢掉 → 各 k 的 `nFolds` 不齐 → `testStd/√nFolds` 的 1-SE 不可比 → 推荐因子数 k\* 偏移。
- **改法**：改 LPT 装箱（按组大小降序，每次放进当前累计样本最少的折）。

---

# P2 ✅ 全部完成（2026-07-29）

测试：641 → 646 全绿；`node tests/lint-strategies.js` 从 1 error/1 warn → 0/0。记录见 [review/readme.md §23.4](review/readme.md)。

## 12. `rebuildFactors` 同时用参数和闭包 ✅

- **位置**：[review/src/ui/factorLab/useFactorScan.js:122](review/src/ui/factorLab/useFactorScan.js)（用参数 `prevFactors`）、`:137`（用闭包 `factors`）
- **说明**：当前所有调用方传的是同一个所以没炸，但只要有人传不同的，`preserved` 和 `merged` 就来自两份不同的池子 → 静默丢因子。二选一即可。

## 13. ✅ `stripComments` 不识别正则字面量 —— **已实测复现，不再是"疑似"**

- **位置**：[review/src/lib/onlineExport.js:832](review/src/lib/onlineExport.js)
- **复现**（2026-07-29，照抄该函数实现跑的最小用例）：

  ```js
  const hasApos = /don't/.test(meta)            // 正则里一个单引号 → 扫描器进入"字符串模式"
  // 说明：以前这里用过 f('已废弃字段')，现在不用了    ← 这行注释因此剥不掉
  const a = f('new_volume')
  ```

  `extractUsedFields` 实际返回 `['已废弃字段', 'new_volume']` —— 把只在注释里出现过的字段当成了真字段。
- **两个方向都会错**：
  - **多提取**（上面这个）：生成的上线代码多一个用不上的 const，`verifyParity` 还会去自检一个策略里根本不存在的字段。
  - **少提取**：形如 `/https:\/\//.test(x)` 的正则，末尾那对 `//` 会被当成行注释起点，**同一行后面的 `f('字段')` 整段被吞** → 上线代码少一个 const、对应因子恒 null。
- **改法**：给这个手写扫描器补正则字面量识别（判断 `/` 是除号还是正则开头：看前一个非空白字符是否属于 `)]}`、标识符或字面量结尾）。
- **同一个根因还导致了下面第 16 条。**

## 14. 死代码与文档矛盾 ✅

- `scoreRow` 的 `opts` 从不解构、`buildFactors` 的 `opts.shape` 从不读，却整条链（UI→hook→workerPool→worker）都在传，还算进 `poolKey` 缓存签名。
- `parseCheckDirection`（[proAnalytics.js:65](review/src/lib/proAnalytics.js)）已无生产调用方，只剩测试在测；函数头注释复制了两遍。
- [CLAUDE.md:304](CLAUDE.md) 注释断言 `volume_ratio` "content 文案里写的是 12.31x"，同段 `:312` 给的 content 样本却写 `12.31%`。**已订正（2026-07-29，用户拍板）**：数值口径无歧义——同一条样本 `current_volume/avg_history_volume = 73.887262466/6.001835 = 12.3108`，与 `volume_ratio` 逐位对上，策略侧不受影响。改法是**只改注释、不断言平台文案**：删掉"对应 content 文案里的 12.31x"，改成写上验算式 + 一行警告（content 文案写的是 "12.31%"，属平台侧措辞，以字段数值为准）。

## 16. ✅ 策略 lint 的规则跑在**未剥注释**的源码上 → 现在唯一的 error 是误报（本轮新发现）

- **位置**：[review/src/lib/strategySpec.js:73](review/src/lib/strategySpec.js) 的 `RULES[].test(c)`，`c` 是原始源码
- **现象**：`node tests/lint-strategies.js` 现在报 `✗ 1.5段策略/code-score.js —— 用了 f('字段') 但没有 f 垫片 → 线上会 f is not defined`，退出码 1。而该文件里三处 `f('` **全在注释里**，其中一处恰恰是在说明"本策略不调用 `f('字段')`，所以不需要垫片"。
- **后果**：① 唯一的 error 级输出是假的，挂 CI 就是长红，人会习惯性忽略真违规；② 这条规则是 `fixable`，UI 上点「修正」会**插入一段根本不需要的垫片**。
- **改法**：`checkStrategySpec` 先剥注释再跑正则（复用第 13 条修好的 `stripComments`，从 `onlineExport.js` 提出来共用）。注意**不能连字符串一起剥**——`f('字段')` 的字段名本身就是字符串字面量。
- **验收**：加测试——"注释里提到 `f('x')` 但代码没调用"不该报违规；"代码真的调用了 `f('x')` 且无垫片"仍然要报。

## 15. 仓库卫生 ✅（只改 .gitignore）

- `review_副本/`、`root@167.99.73.172/`、`review/js/`（旧版原生 js）、`review/dist/`、若干 `.DS_Store`、根目录 `top100_calls.json`/`worst100_calls.json` 都在 untracked 里；`.gitignore` 只有一行。
- **只改 `.gitignore`，不删任何目录**——删除不可逆，等用户确认。

---

# 不改的部分（明确记录，避免下次重复审）

- `pApprox` 用 CI 半宽反推 p 值（[auc.js:60](review/src/lib/auc.js)）：方法上确实是近似（隐含 bootstrap 分布对称正态），但它只用于 BH 排序和一个展示用判定列，改成精确检验要引入更重的计算。**记录为已知取舍，不在本轮改。**
- readme §8.7 已自行记录的已知未修项（`recommendFactorPath` 用 test 段贪心、两阵营 score 不同量纲、两套 train/test 切分口径、`bootstrapAucCI` 共用种子）——本轮不动。
- 工程判断做得好、不要动：`PlotlyChart.jsx` 清监听器、`PerfMonitor.jsx` 的 effect cleanup、`onlineExport` 逐字段 parity 自检设计、`scanIntervalCore` 用置换检验对付 look-elsewhere、`tests/online-export-coverage.test.js` 的静态扫描式守门（建议把这个模式推广到「两处必须同步」的地方，比如本轮的 `direction` 常量和 `ci` 数据形状）。
